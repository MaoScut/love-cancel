虽然已经可以分批次地消除元素了，但是想加动画是不行的，因为目前是通过设置每个元素的颜色来实现消除的，实际上元素的位置没有移动，这种方式不能通过加css来实现动画

元素自动填满的思路，计算每一列要补充的元素的个数，先在顶部生成，然后再一起下降

真正实现每个元素位置的移动，要做如下改变
1.原来矩阵的元素是数字，现在要变成一个对象，包含key和color
2.消除元素时，判断条件变为元素的color属性
3.调整矩阵也同理
4.有了key属性后，react就会把之前的之后的元素当作是同一个，应该能触发动画效果
---2017,7,12

添加了类之后，不涉及消除元素时候，有动画效果，但是涉及到元素消除时候，就没有了，怎么回事？
元素简单交换，为什么一个有动画，一个没有？
往下拖动，被动交换的元素有动画
往上拖动，主动交换的元素有动画
往右拖动，被动交换的元素有动画
往左拖动，主动交换的元素有动画
很奇怪，所有的元素都是根据矩阵来渲染的，为什么会有区别呢？
先解决为什么交换的时候，一个有动画，另外一个没有。
为了看看key属性到底怎么运作，写个简单的组件
简单组件也呈现只有一个会有动画
一上午都没找到解决方案
